# lug-nuts

A (p)lug-and-play wrapper around the No U-Turn Sampler implementation found in `numpyro`. Just provide functions for your likelihood and its gradient (not necessarily written with JAX), a configuration file describing your model parameters, and let it run.
